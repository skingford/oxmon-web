# oxmon 管理后台 — 产品设计规格书

> 设计风格：Apple Light Theme | 桌面端优先（>=1024px）| 极简专业

---

## 1. 设计语言

### 1.1 色彩系统

| Token | 色值 | 用途 |
|-------|------|------|
| `bg-primary` | `#FFFFFF` | 卡片、侧边栏、输入框背景 |
| `bg-secondary` | `#F5F5F7` | 页面内容区背景 |
| `text-primary` | `#1D1D1F` | 标题、正文主文字 |
| `text-secondary` | `#6E6E73` | 辅助说明文字、表头 |
| `accent-blue` | `#0071E3` | 主按钮、链接、选中态、info 徽章 |
| `success-green` | `#34C759` | 在线状态、有效证书、成功态 |
| `warning-orange` | `#FF9500` | warning 告警、证书即将过期 |
| `danger-red` | `#FF3B30` | critical 告警、离线状态、证书无效 |
| `border` | `#D2D2D7` | 输入框边框、分割线 |
| `border-light` | `#E5E5EA` | 表格行分割、图表网格线 |

### 1.2 字体

| 属性 | 规范 |
|------|------|
| 字体族 | SF Pro Display → Inter → system-ui |
| 页面标题 | 24px / font-weight: 600 / `#1D1D1F` |
| 卡片标题 | 16px / font-weight: 600 / `#1D1D1F` |
| 正文 | 14px / font-weight: 400 / `#1D1D1F` |
| 辅助文字 | 13px / font-weight: 400 / `#6E6E73` |
| 表头 | 12px / font-weight: 500 / `#6E6E73` / text-transform: uppercase |
| 数字/指标值 | 32px / font-weight: 700 / `#1D1D1F` |

### 1.3 圆角与阴影

| 元素 | 圆角 | 阴影 |
|------|------|------|
| 卡片 | 12px | `0 2px 12px rgba(0,0,0,0.08)` |
| 按钮 | 8px | 无 |
| 输入框 | 8px | 无（border: 1px solid `#D2D2D7`） |
| 模态框 | 16px | `0 8px 32px rgba(0,0,0,0.12)` |
| 徽章 (Badge) | 4px | 无 |
| 头像/状态点 | 50% (圆形) | 无 |

### 1.4 间距系统

基于 8px 网格：

| 名称 | 值 | 用途 |
|------|----|------|
| xs | 4px | 图标与文字间距 |
| sm | 8px | 紧凑元素间距 |
| md | 16px | 组件内部 padding |
| lg | 24px | 卡片间距、区块分隔 |
| xl | 32px | 页面内容区 padding |
| 2xl | 48px | 大区块分隔 |

### 1.5 按钮样式

| 类型 | 背景 | 文字 | 边框 | 悬停态 |
|------|------|------|------|--------|
| Primary | `#0071E3` | `#FFFFFF` | 无 | `#0077ED`（加深） |
| Secondary | `#FFFFFF` | `#0071E3` | 1px solid `#0071E3` | `#F0F7FF` 背景 |
| Danger | `#FF3B30` | `#FFFFFF` | 无 | `#E6352B` |
| Ghost | transparent | `#6E6E73` | 无 | `#F5F5F7` 背景 |
| Disabled | `#E5E5EA` | `#AEAEB2` | 无 | 不可点击 |

按钮高度统一 36px，内边距 `0 16px`，字号 14px / font-weight: 500

---

## 2. 全局布局

### 2.1 整体结构

```
┌──────────────────────────────────────────────────────┐
│ ┌──────────┐ ┌─────────────────────────────────────┐ │
│ │          │ │  Header (56px)         [用户名] [退出] │ │
│ │  Sidebar │ ├─────────────────────────────────────┤ │
│ │  (240px) │ │                                     │ │
│ │          │ │  Content Area                       │ │
│ │  Logo    │ │  bg: #F5F5F7                        │ │
│ │          │ │  padding: 32px                      │ │
│ │  Nav     │ │  max-width: 1280px                  │ │
│ │  Items   │ │                                     │ │
│ │          │ │                                     │ │
│ │          │ │                                     │ │
│ └──────────┘ └─────────────────────────────────────┘ │
└──────────────────────────────────────────────────────┘
```

### 2.2 侧边栏 (Sidebar)

- 宽度：240px，固定定位
- 背景：`#FFFFFF`
- 右边框：1px solid `#E5E5EA`
- 顶部：oxmon Logo（距顶 24px，水平居中）
- 导航项高度：44px，左 padding 24px
- 导航项图标：20px，与文字间距 12px
- 导航项文字：14px / font-weight: 500
- 默认态：`#6E6E73` 文字
- 悬停态：`#F5F5F7` 背景
- 选中态：`#0071E3` 文字 + `#E8F0FE` 背景 + 左侧 3px `#0071E3` 竖线

导航项列表：
1. **Dashboard** — 图标: grid/chart icon
2. **Agents** — 图标: server/cpu icon
3. **Metrics** — 图标: line-chart icon
4. **Alerts** — 图标: bell icon
5. **Certificates** — 图标: shield/lock icon

### 2.3 顶栏 (Header)

- 高度：56px
- 背景：`#FFFFFF`
- 底边框：1px solid `#E5E5EA`
- 右侧内容：用户名（14px / `#1D1D1F`）+ 退出按钮（Ghost 样式）
- 左侧：当前页面标题（18px / font-weight: 600）

---

## 3. 页面详细规格

### 3.1 登录页 (Login)

**路由：** `/login`
**布局：** 无侧边栏，全屏独立页面

```
┌──────────────────────────────────────┐
│              bg: #F5F5F7             │
│                                      │
│        ┌──────────────────┐          │
│        │    oxmon Logo     │          │
│        │                  │          │
│        │  ┌────────────┐  │          │
│        │  │  用户名      │  │          │
│        │  └────────────┘  │          │
│        │  ┌────────────┐  │          │
│        │  │  密码        │  │          │
│        │  └────────────┘  │          │
│        │                  │          │
│        │  ┌────────────┐  │          │
│        │  │   登 录     │  │          │
│        │  └────────────┘  │          │
│        │                  │          │
│        └──────────────────┘          │
│         max-width: 400px             │
│         card 样式 (白底/12px圆角)      │
└──────────────────────────────────────┘
```

**规格：**
- 卡片：max-width 400px，居中，padding 40px
- Logo：居中，底部 margin 32px
- 输入框：宽度 100%，高度 44px，border 1px `#D2D2D7`，圆角 8px，padding 0 12px
- 输入框 placeholder：`#AEAEB2`
- 输入框 focus：border `#0071E3`，shadow `0 0 0 3px rgba(0,113,227,0.15)`
- 输入框间距：16px
- 登录按钮：宽度 100%，高度 44px，Primary 样式，顶部 margin 24px
- 错误信息：14px / `#FF3B30`，输入框下方 4px
- 全局错误：卡片顶部，`#FFF2F0` 背景，`#FF3B30` 文字，padding 12px，圆角 8px

**交互：**
- 空字段提交 → 对应输入框下方显示红色提示 "请输入用户名" / "请输入密码"
- 登录失败 → 卡片顶部显示 "用户名或密码错误"
- 登录成功 → 跳转 `/dashboard`

---

### 3.2 Dashboard 总览页

**路由：** `/dashboard`

```
┌──────────────────────────────────────────┐
│  Dashboard                               │
│                                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │ Agent 在线 │ │ 版本信息  │ │ 运行时间  │  │
│  │           │ │          │ │          │  │
│  │   3/5     │ │  v0.1.0  │ │  3 天    │  │
│  │   60%     │ │          │ │          │  │
│  └──────────┘ └──────────┘ └──────────┘  │
│                                          │
│  ┌──────────────────────────────────────┐ │
│  │  最近告警                              │ │
│  │                                      │ │
│  │  🔴 critical  web-01  cpu.usage ...  │ │
│  │  🟡 warning   db-01   memory ...     │ │
│  │  🔵 info      web-02  disk ...       │ │
│  │  ...                                 │ │
│  └──────────────────────────────────────┘ │
└──────────────────────────────────────────┘
```

**概览卡片行：** 3 列等宽，间距 24px

**Agent 在线率卡片：**
- 标题："Agent 在线率"（13px / `#6E6E73`）
- 主数值："3 / 5"（32px / 700 weight）
- 百分比："60%"（24px / `#34C759` 绿色或 `#FF3B30` 红色，阈值 50%）
- 底部状态条：总宽100%，绿色部分占比 = 在线率

**版本信息卡片：**
- 标题："服务版本"
- 数值：version 字符串

**运行时间卡片：**
- 标题："运行时间"
- 数值：格式化为 "X 天 Y 小时" 或 "X 小时 Y 分钟"

**最近告警列表：**
- 卡片标题："最近告警"
- 列表最多 10 条，每条高度 48px
- 每行：严重级别徽章 + Agent ID + 指标名 + 消息 + 相对时间
- 空状态：居中图标 + "暂无告警记录"（`#6E6E73`）

---

### 3.3 Agent 列表页

**路由：** `/agents`
**Tab 切换：** "Agent 列表" | "白名单"

#### Tab: Agent 列表

**表格列：**

| 列名 | 宽度 | 内容 |
|------|------|------|
| Agent ID | 自适应 | 文字，可点击（蓝色链接色） |
| 状态 | 100px | Badge：在线=绿色实心/离线=灰色 |
| 最后上报 | 180px | 相对时间（"3 分钟前"） |

- 行高 52px
- 行悬停：`#F5F5F7` 背景
- 点击整行跳转到 `/agents/:id`

#### Tab: 白名单

**操作栏：** 页面标题右侧 "添加 Agent" 按钮（Primary）

**表格列：**

| 列名 | 宽度 | 内容 |
|------|------|------|
| Agent ID | 自适应 | 文字 |
| 描述 | 自适应 | 文字 |
| 状态 | 100px | 在线/离线 Badge |
| 创建时间 | 160px | YYYY-MM-DD HH:mm |
| 操作 | 240px | 编辑(Ghost) / 重新生成 Token(Ghost) / 删除(Danger Ghost) |

**添加 Agent 模态框：**
- 标题："添加 Agent"
- 字段：Agent ID（必填）、描述（可选）
- 按钮：取消(Secondary) + 确定(Primary)
- 成功后弹出 Token 展示模态框

**Token 展示模态框：**
- 标题："Token 已生成"
- 警告横幅：`#FFF8E1` 背景 + `#FF9500` 图标 + "Token 仅显示一次，请妥善保存"
- Token 文本区域：等宽字体，`#F5F5F7` 背景，padding 16px，可选中
- 复制按钮：点击后文案变为 "已复制"（1.5s 后恢复）

**删除确认模态框：**
- 标题："确认删除"
- 内容："确定要删除 Agent「{agent_id}」吗？此操作不可撤销。"
- 按钮：取消(Secondary) + 删除(Danger)

**重新生成 Token 确认框：**
- 标题："重新生成 Token"
- 内容："重新生成后旧 Token 将立即失效，Agent 需要更新配置。确定继续？"
- 按钮：取消(Secondary) + 确认(Primary)
- 成功后弹出 Token 展示模态框

---

### 3.4 Agent 详情页

**路由：** `/agents/:id`

```
┌──────────────────────────────────────────┐
│  ← 返回 Agent 列表    Agent: web-01      │
│                                          │
│  ┌──────────┐ ┌──────────┐               │
│  │ CPU 使用率 │ │ 内存使用率 │               │
│  │           │ │          │               │
│  │  45.2%    │ │  72.8%   │               │
│  └──────────┘ └──────────┘               │
│  ┌──────────┐ ┌──────────┐               │
│  │ 磁盘使用率 │ │ 负载均值  │               │
│  │           │ │          │               │
│  │  58.3%    │ │  1.24    │               │
│  └──────────┘ └──────────┘               │
└──────────────────────────────────────────┘
```

**指标卡片：** 2x2 网格，间距 24px

每张卡片：
- 标题：13px / `#6E6E73`（"CPU 使用率"、"内存使用率"、"磁盘使用率"、"负载均值"）
- 数值：32px / 700 weight / `#1D1D1F`
- 数值带单位：百分比指标加 "%"
- 超过阈值变色：>80% 用 `#FF9500`，>90% 用 `#FF3B30`

**Agent 未找到：**
- 空状态组件
- 图标 + "Agent 未找到"
- "返回 Agent 列表" 链接

---

### 3.5 指标查询页 (Metrics)

**路由：** `/metrics`

```
┌──────────────────────────────────────────┐
│  指标查询                                  │
│                                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │ Agent ▾  │ │ 指标名  ▾  │ │ 查询     │  │
│  └──────────┘ └──────────┘ └──────────┘  │
│                                          │
│  [ 1小时 ] [ 6小时 ] [ 24小时 ] [ 7天 ]   │
│  [ 自定义:  ______ ~ ______ ]            │
│                                          │
│  ┌──────────────────────────────────────┐ │
│  │                                      │ │
│  │         📈 折线图区域                  │ │
│  │         line: #0071E3                │ │
│  │         area fill: rgba(0,113,227,   │ │
│  │                         0.08)        │ │
│  │         grid: #E5E5EA               │ │
│  │                                      │ │
│  └──────────────────────────────────────┘ │
└──────────────────────────────────────────┘
```

**筛选栏：**
- Agent 下拉选择：动态加载（来自 API）
- 指标下拉选择：静态列表
  - cpu.usage
  - memory.used_percent
  - disk.used_percent
  - load.load_1 / load.load_5 / load.load_15
  - network.bytes_sent / network.bytes_recv
- 查询按钮：Primary 样式
- 验证：未选择时输入框标红 + 下方提示

**时间范围：**
- 预设按钮组：1 小时 / 6 小时 / 24 小时 / 7 天
- 选中按钮：`#0071E3` 背景 + 白色文字
- 未选中：`#FFFFFF` 背景 + `#1D1D1F` 文字 + 1px `#D2D2D7` 边框
- 自定义：两个 datetime 输入框，中间 "~" 分隔

**图表规格：**
- 背景：`#F5F5F7`
- 折线颜色：`#0071E3`，线宽 2px
- 面积填充：`rgba(0,113,227,0.08)`
- 网格线：`#E5E5EA`，虚线
- X 轴：时间戳，12px `#6E6E73`
- Y 轴：数值，12px `#6E6E73`
- Tooltip：白底卡片样式，显示精确时间 + 值

---

### 3.6 告警页 (Alerts)

**路由：** `/alerts`
**Tab 切换：** "告警规则" | "告警历史"

#### Tab: 告警规则

**表格列：**

| 列名 | 宽度 | 内容 |
|------|------|------|
| 规则名称 | 自适应 | 文字 |
| 类型 | 140px | threshold / rate_of_change / trend_prediction / cert_expiration |
| 指标 | 150px | 指标名 |
| Agent 匹配 | 150px | glob 模式 |
| 严重级别 | 100px | 色彩 Badge |
| 参数 | 自适应 | JSON 简短展示 |

#### Tab: 告警历史

**筛选栏：**
- 严重级别下拉：全部 / critical / warning / info
- Agent 输入框：文本过滤
- 时间范围：开始日期 ~ 结束日期

**表格列：**

| 列名 | 宽度 | 内容 |
|------|------|------|
| 严重级别 | 80px | 色彩 Badge |
| Agent | 150px | Agent ID |
| 指标 | 150px | 指标名 |
| 消息 | 自适应 | 告警消息文本 |
| 值/阈值 | 120px | 触发值 / 阈值 |
| 时间 | 180px | YYYY-MM-DD HH:mm:ss |

**分页控件：**
- 位于表格底部，右对齐
- 页码按钮：« 上一页 / [1] [2] [3] / 下一页 »
- 每页条数选择器：10 / 25 / 50 / 100
- 样式：小号按钮，当前页 `#0071E3` 背景

**严重级别 Badge 规格：**

| 级别 | 背景色 | 文字 |
|------|--------|------|
| critical | `#FF3B30` | `#FFFFFF` |
| warning | `#FF9500` | `#FFFFFF` |
| info | `#0071E3` | `#FFFFFF` |

Badge 样式：padding `2px 8px`，圆角 4px，字号 12px / font-weight: 500

---

### 3.7 证书监控页 (Certificates)

**路由：** `/certificates`
**Tab 切换：** "域名管理" | "证书状态"

#### Tab: 域名管理

**操作栏：** "添加域名"(Primary) + "批量添加"(Secondary) + "全部检测"(Secondary)

**表格列：**

| 列名 | 宽度 | 内容 |
|------|------|------|
| 域名 | 自适应 | 文字 |
| 端口 | 80px | 数字（默认 443） |
| 启用 | 80px | Toggle 开关 |
| 检查间隔 | 100px | 格式化时间 |
| 备注 | 自适应 | 文字 |
| 最后检查 | 160px | 相对时间 |
| 操作 | 200px | 编辑(Ghost) / 立即检测(Ghost) / 删除(Danger Ghost) |

**搜索与过滤：**
- 搜索框：表格上方左侧，placeholder "搜索域名..."
- 过滤按钮："30 天内过期"（点击切换筛选状态）

**添加域名模态框：**
- 标题："添加域名"
- 字段：域名（必填）、端口（默认 443）、备注（可选）
- 按钮：取消(Secondary) + 添加(Primary)

**批量添加模态框：**
- 标题："批量添加域名"
- 内容：textarea，placeholder "每行一个域名"
- 辅助文字："每行输入一个域名，默认端口 443"
- 按钮：取消(Secondary) + 添加(Primary)

#### Tab: 证书状态

**表格列：**

| 列名 | 宽度 | 内容 |
|------|------|------|
| 域名 | 自适应 | 可点击跳转详情（蓝色链接色） |
| 有效 | 80px | ✓ 绿色圆圈 / ✗ 红色圆圈 |
| 颁发者 | 自适应 | Issuer 信息 |
| 剩余天数 | 120px | 数字 + 颜色编码 |
| 最后检查 | 160px | 相对时间 |

**剩余天数颜色：**
- \> 30 天：`#34C759`（绿色）
- 7 - 30 天：`#FF9500`（橙色）
- < 7 天：`#FF3B30`（红色）

---

### 3.8 证书详情页

**路由：** `/certificates/:id`

```
┌──────────────────────────────────────────┐
│  ← 返回证书列表    example.com            │
│                                          │
│  ┌──────────────────────────────────────┐ │
│  │  证书基本信息                          │ │
│  │                                      │ │
│  │  域名          example.com           │ │
│  │  生效时间       2025-01-01            │ │
│  │  过期时间       2026-01-01            │ │
│  │  剩余天数       327 天  (绿色)         │ │
│  │  颁发者 CN     Let's Encrypt          │ │
│  │  颁发者组织     Let's Encrypt          │ │
│  │  证书链有效     ✓ 有效                  │ │
│  └──────────────────────────────────────┘ │
│                                          │
│  ┌──────────────────────────────────────┐ │
│  │  SAN 列表                             │ │
│  │  • example.com                       │ │
│  │  • *.example.com                     │ │
│  └──────────────────────────────────────┘ │
│                                          │
│  ┌──────────────────────────────────────┐ │
│  │  IP 地址                              │ │
│  │  • 1.2.3.4                           │ │
│  │  • 5.6.7.8                           │ │
│  └──────────────────────────────────────┘ │
│                                          │
│  [ 查看证书链 ]                            │
│                                          │
│  ┌──────────────────────────────────────┐ │
│  │  证书链验证                            │ │
│  │  状态: ✓ 链验证通过                    │ │
│  │  （若失败显示错误信息）                   │ │
│  └──────────────────────────────────────┘ │
└──────────────────────────────────────────┘
```

**详情信息卡片：**
- 使用两列 key-value 布局
- Key：13px / `#6E6E73`，宽 120px
- Value：14px / `#1D1D1F`
- 行高 36px

---

## 4. 通用组件规格

### 4.1 Card 组件

```
背景: #FFFFFF
圆角: 12px
阴影: 0 2px 12px rgba(0,0,0,0.08)
内边距: 24px
```

### 4.2 Table 组件

```
表头: bg #F5F5F7, 文字 12px/500/#6E6E73, 高度 40px, padding 0 16px
行: bg #FFFFFF, 文字 14px/400/#1D1D1F, 高度 52px, padding 0 16px
行分割: 1px solid #E5E5EA
行悬停: bg #F5F5F7
```

### 4.3 Modal 组件

```
遮罩: rgba(0,0,0,0.4)
容器: bg #FFFFFF, 圆角 16px, 阴影 0 8px 32px rgba(0,0,0,0.12)
宽度: 480px (居中)
标题: 18px/600/#1D1D1F, 底部 border 1px #E5E5EA
内容: padding 24px
底部按钮栏: padding 16px 24px, 右对齐, 按钮间距 12px
```

### 4.4 Badge 组件

```
padding: 2px 8px
圆角: 4px
字号: 12px / font-weight: 500
颜色: 白色文字 + 对应背景色
```

### 4.5 Toggle 开关

```
宽度: 44px, 高度: 24px
关闭态: bg #E5E5EA, 圆形滑块白色
开启态: bg #34C759, 圆形滑块白色
滑块: 20px 圆形, 2px 间距
过渡: 0.2s ease
```

### 4.6 EmptyState 组件

```
垂直居中
图标: 48px, #D2D2D7
文字: 16px / #6E6E73, margin-top 16px
```

### 4.7 Tab 组件

```
容器: 底部 border 1px #E5E5EA
Tab 项: padding 12px 24px, 14px/500
默认态: #6E6E73 文字
选中态: #0071E3 文字 + 底部 2px #0071E3 下划线
悬停态: #1D1D1F 文字
```

---

## 5. 页面清单与路由

| 路由 | 页面 | 主要功能 |
|------|------|---------|
| `/login` | 登录页 | JWT 登录（无侧边栏） |
| `/dashboard` | 总览页 | Agent 在线率、版本/运行时间、最近告警 |
| `/agents` | Agent 列表 | Agent 表格 + 白名单管理 Tab |
| `/agents/:id` | Agent 详情 | CPU/内存/磁盘/负载指标卡片 |
| `/metrics` | 指标查询 | Agent+指标选择、时间范围、折线图 |
| `/alerts` | 告警管理 | 告警规则表格 + 告警历史 Tab（过滤+分页） |
| `/certificates` | 证书监控 | 域名管理 CRUD + 证书状态 Tab |
| `/certificates/:id` | 证书详情 | 证书完整信息、SAN、IP、证书链验证 |

---

## 6. 状态设计

### 6.1 加载态

- 页面级：居中旋转 Spinner（24px，`#0071E3`）+ "加载中..."
- 按钮级：按钮内 Spinner 替换文字，禁用点击
- 表格级：3-5 行 Skeleton（`#F5F5F7` 到 `#E5E5EA` 渐变动画）

### 6.2 错误态

- 居中图标（叹号圆圈，48px，`#FF3B30`）
- 错误信息：14px / `#1D1D1F`
- 重试按钮：Secondary 样式

### 6.3 空数据态

- 居中图标（对应场景的空状态图标，48px，`#D2D2D7`）
- 提示文案：16px / `#6E6E73`
